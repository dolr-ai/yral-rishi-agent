# Pre-spawn contracts from coordinator — yral-rishi-agent-influencer-and-profile-directory

> **Provenance:** This file preserves the placeholder `README.md` the coordinator
> pre-staged for this service folder. The placeholder `README.md` was removed
> during the Day-1 spawn (per `new-service.sh`'s no-overwrite guard) and replaced
> with the template's standard `README.md`. The pre-spawn placeholder did NOT
> contain substantive engineering contracts beyond the standard "see plan-and-
> discussions for the full plan" pointer (verifiable via `git log --follow`
> across the spawn PR), so there is no contract content to preserve here.
> This stub exists for consistency with the other two services Session 4 spawned
> on the same day (the orchestrator + soul-file-library both preserved real
> contract content this way).

## RELATED FILES
- `README.md` — the template-spawned service README (replaces the pre-spawn placeholder).
- `yral-rishi-agent-plan-and-discussions/` — full plan, constraints, timeline.
- `yral-rishi-agent-plan-and-discussions/multi-session-parallel-build-coordination/interface-contracts/00-api-contract.md` — the InfluencerDto + endpoint contract this service implements.
