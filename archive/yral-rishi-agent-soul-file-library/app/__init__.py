# ---------------------------------------------------------------------------
# app/ — the Python package that holds the running service's code.
#
# This file is intentionally empty. Its presence marks `app/` as a Python
# package so that `import app.main`, `import app.sentry_middleware`, etc.
# work. Modules are added one at a time (one per Day-2 PR).
#
# RELATED FILES:
#   main.py              — FastAPI app + lifespan
#   sentry_middleware.py — Sentry init (per A7 + D3)
# ---------------------------------------------------------------------------
