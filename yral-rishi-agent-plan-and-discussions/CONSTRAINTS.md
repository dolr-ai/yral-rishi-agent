# V2 Chat — Hard Constraints (reviewable, one per row)

> The tight, go/no-go list of every hard constraint the v2 build operates under. Nothing on this list is aspirational — each row is non-negotiable unless Rishi explicitly lifts it. Reviewed item-by-item before any code is written.
>
> Status legend: 🔒 locked (cannot change without explicit Rishi reversal) · 🟡 provisional (needs Rishi confirm) · 🟢 resolved (answered, folded into plan)
>
> Related docs:
> - [`CURRENT-TRUTH.md`](./CURRENT-TRUTH.md) — single-source-of-agreement for build mode, mobile URL, paywall contract, routing path, phase order. Read this if other docs disagree
> - [`README.md`](./README.md) — full v2 plan (1500+ lines): capability blueprints, roadmap, architecture
> - [`V2_TEMPLATE_AND_CLUSTER_PLAN.md`](./V2_TEMPLATE_AND_CLUSTER_PLAN.md) — template + rishi-4/5/6 cluster details

---

## Category A — The Safety Covenant (the "do not break these or everything breaks")

| # | Constraint | Status | Source | How we verify |
|---|---|---|---|---|
| A1 | **No deletions** of ANY existing artifact (repos, stacks, DNS records, Caddy routes, Postgres tables, GitHub secrets, memory files, servers) without explicit per-item Rishi approval | 🔒 | `feedback_no_delete_without_asking.md`; Rishi 2026-04-22 | Every proposed deletion has an explicit YES from Rishi in chat before executing; double-confirmation required |
| A2 | **Rishi-1/2/3 are LIVE production servers — hands off BY DEFAULT.** Read-only access (key: `~/.ssh/rishi-hetzner-ci-key`, user `deploy`); NO writes, NO deploys, NO config changes. **EXCEPTION (clarified 2026-04-24):** Rishi personally owns the Caddy config on rishi-1/2 (deployed weeks ago via the `yral-rishi-hetzner-infra-template` repo). V2 routing snippets (e.g., `agent.rishi.yral.com` → rishi-4/5 upstream) are added via that repo's Caddy snippet pattern, not by hand-editing on the server. All other rishi-1/2/3 resources (Python chat-ai stack, Sentry, metabase, existing snippets) remain untouched | 🔒 | Rishi 2026-04-23/24 | Any access that would write to rishi-1/2/3 BEYOND "add a v2 Caddy snippet via the template repo" requires typed approval. Caddy snippet adds: PR against `yral-rishi-hetzner-infra-template`, CI green, `docker exec caddy caddy reload --force` on rishi-1/2 |
| A3 | **One mobile-client change maximum.** Mobile base URL stays at `chat-ai.rishi.yral.com` through cutover. Streaming (v2.0) + Plan B.0 presence heartbeat are the approved mobile changes — bundled | 🔒 | Rishi 2026-04-22/23 | Section 11.8.1 of README enumerates exact mobile asks; anything not listed there = needs Sarvesh/Shivam + Saikat re-approval |
| A4 | **All data MUST port — AI influencers AND user chat history.** No exceptions. Applies to LOCAL TESTING (immediate) AND PRODUCTION CUTOVER (when Rishi says). Reversed 2026-04-24 from earlier "chat history may be discarded" — Rishi: "losing chat data would hamper user experience for current users, that would be the wrong approach." Influencer ID, Soul File, creator link, earnings, followers, full conversation history, messages, read states — all carry forward. V2 schema is greenfield; ETL transforms chat-ai rows into whatever shape v2 needs; no rows dropped | 🔒 | Rishi 2026-04-24 (REVERSED from 2026-04-22); `feedback_greenfield_and_partial_build_authorization_2026_04_24.md` | Migration script inventoried in README Section 7 Step 2; pre-cutover count check: `SELECT COUNT(*) FROM messages` in chat-ai == `SELECT COUNT(*) FROM conversation.messages` in v2 (+/- rows created during cutover window) |
| A5 | **Plan-only mode until Rishi types "build".** No code, no repo creation, no server provisioning, no CI pushes for v2 services until explicit build approval | 🔒 | `feedback_plan_only_until_explicit_build_approval.md`; Rishi 2026-04-23 | No actions taken that materialize the plan into running systems; this doc, README, memories all editable — nothing else |
| A6 | **No cutover until Rishi explicitly says "cut over now".** V2 exists alongside BOTH Ravi's Rust `yral-ai-chat` AND our Python `yral-chat-ai` (the latter WENT LIVE 2026-04-23 and is handling production load; users are reporting slow latency — directly motivates E1). No timeline. No pressure. Cutover happens only when Rishi is satisfied v2 is "1000× better" than both current services. Do NOT raise cutover as a topic until he does | 🔒 | Rishi 2026-04-23/24 | Any text I write that references cutover timing must be prefixed "at Rishi's discretion" or removed; no phase gate or deadline mentions a cutover |
| A7 | **Sentry for ALL v2 services is `sentry.rishi.yral.com`** (Rishi's self-hosted on rishi-3), NEVER the team-shared `apm.yral.com`. Reinforced 3 times by Rishi now (2026-04-22, 2026-04-23 morning, 2026-04-23 evening) — this is a CRITICAL memory item | 🔒 | Rishi 2026-04-22/23 (multiple reminders) | Every service's Sentry DSN points at `sentry.rishi.yral.com`; CI lint can verify the DSN host matches this; template README opens with this reminder |
| A8 | **Feature parity with existing services is a HARD constraint.** Every endpoint / capability / behavior currently live in Ravi's Rust `yral-ai-chat` AND our Python `yral-chat-ai` (LIVE since 2026-04-23) MUST be preserved in v2. No silent regressions. Includes: full influencer CRUD including 3-step creation flow, system-prompt editing, video-prompt generation, admin ban/unban, WebSocket inbox, multi-modal messages, H2H scaffolding, Chat-as-Human, 50-msg paywall, metadata-service push notifications, Google Chat webhook for admin events | 🔒 | Rishi 2026-04-23 evening | Audit lives in `feature-parity-with-existing-chat-services-audit/feature-parity-audit.md`. V2 public-api contract tests include every existing endpoint. Shadow-traffic comparison flags any divergence |
| A9 | **Test v2 on the REAL YRAL mobile Android app via the REAL cluster from Day 8+ (refined 2026-04-24 evening).** Target path: debug APK built BY CLAUDE from `~/Claude Projects/yral-mobile/` via `./gradlew assembleDebug`, installed on Rishi's Motorola via `adb install`. Debug APK's `CHAT_BASE_URL` points at `agent.rishi.yral.com` (NOT laptop, NOT Cloudflare Tunnel) — real cluster from Day 8+ after Phase 0 cluster provisioning completes. Route: Cloudflare DNS → rishi-1/2 Caddy snippet → rishi-4/5 Swarm ingress. Debug APK ONLY talks to v2 (never v1); prod Play Store YRAL app stays installed separately for comparison. Every Motorola checkpoint uses the real cluster — production-shape from the first test. Tier 0 browser debug page is optional dev convenience, NOT the primary loop. Workflow A12 (local-only, never push to yral-mobile origin) governs every APK change | 🔒 | Rishi 2026-04-24 evening (refined from "Cloudflare Tunnel to laptop" to "real cluster from Day 8+") | Detailed in TIMELINE Phase 0 + Phase 1. Day 8 exit criterion = Motorola debug APK does a full turn against `agent.rishi.yral.com` successfully |
| A10 | **Tara stays on OpenRouter via DUAL routing system.** llm-client resolves provider in priority order: (1) per-influencer-id rule (Tara's row in `llm_routing_rule` table) → OpenRouter with her current model; (2) `influencer.is_nsfw=TRUE` → OpenRouter (preserves existing NSFW routing from yral-chat-ai); (3) archetype/turn-type defaults → Gemini Flash + Claude for crisis. This dual approach preserves the existing `is_nsfw` behavior AND adds per-influencer overrides without schema changes | 🔒 | Rishi 2026-04-23 evening + Claude decision 2026-04-23 (audit-driven option b) | Routing matrix in `llm-routing-matrix-with-tara-openrouter-preservation/llm-routing-per-influencer.md`. The resolver function in llm-client checks per-id table first, then is_nsfw flag, then defaults |
| A11 | **Programmatic AI influencer creation is a net-new priority** (priority 5 in F15). Existing endpoints (`POST /influencers/create`, `/generate-prompt`, `/validate-and-generate-metadata`, `/system-prompt`) get wrapped in both (a) an open HTTP API for external agents/tools to create bots programmatically and (b) an MCP-compatible interface for Claude-Desktop-style integrations | 🔒 | Rishi 2026-04-23 evening | Owned by `yral-rishi-agent-influencer-and-profile-directory` + `yral-rishi-agent-skill-runtime` (for MCP wrapper) |
| A12 | **Mobile changes: strict local-first one-at-a-time workflow.** All changes to yral-mobile happen in `~/Claude Projects/yral-mobile/` locally. NEVER push, commit to origin, create a branch remotely, or open a PR. One change at a time. Each change fully documented in `mobile-client-change-log.md` (what, why, how v2 consumes it, what might break, test evidence). Tested on Rishi's Motorola before moving to next change. Once all changes validated, Rishi walks Sarvesh through a detailed doc on a call; PRs go one-at-a-time into YRAL Alpha → production | 🔒 | Rishi 2026-04-24 | `feedback_mobile_change_workflow_one_at_a_time_local_first.md`; change-log doc in umbrella coordination folder; `git push` from yral-mobile working copy is the violation signal |
| A13 | **Build-mode lift (2026-04-24, evening-refined):** Days 1-3 LOCAL template dev on laptop (Docker Compose, localhost). Days 4-7 rishi-4/5/6 cluster provisioning (bootstrap + Swarm + Patroni + Redis Sentinel + Langfuse + Caddy Swarm service + chaos tests per H3). Days 7-8 rishi-1/2 v2 Caddy snippet added via `yral-rishi-hetzner-infra-template` (Rishi-owned Caddy, per A2 exception). Day 8+ cluster is the deploy target; Motorola hits real cluster via `agent.rishi.yral.com`. Mobile changes per A12 (local-only, never pushed) AUTHORIZED. Fully greenfield: v2 backend architecture, schema, DB design need not match v1 — design from first principles for the 50%-faster + 1000×-better goal. MUST preserve all v1 features + AI influencer data + full user chat history (see A4 — REVERSED 2026-04-24: chat data MUST port, not discardable) | 🔒 | Rishi 2026-04-24 evening | Claude decides intra-phase sequencing; TIMELINE.md has the day-by-day. Every architectural choice framed against "does this serve the 50%-faster + 1000×-better + first-principles goal?" — not "does this match v1?" |
| A14 | **Pulling data from live yral-chat-ai production requires explicit Rishi approval EVERY TIME.** v2 dev may need chat-ai data (latency baselines, representative test data, eval prompt curation, influencer catalog snapshots). Authorized pattern: Claude proposes the exact read operation (e.g., "run `pg_dump --schema-only` via ssh rishi-1 `deploy@…`", or "fetch Sentry `events.stats` for endpoint X for last 7 days via API key"), Rishi types YES, operation runs once, results documented in the umbrella. No standing read permission. No silent periodic pulls. Sentry API access (key delivered 2026-04-24) is exempt: reading aggregated perf/error data is pre-authorized | 🔒 | Rishi 2026-04-24 | Every live-data read appears in umbrella with timestamp + Rishi's YES; any violation = stop work immediately |
| A15 | **Motorola testing path: rishi-1/2 Caddy → rishi-4/5 cluster (REFINED 2026-04-24 evening, replaces earlier Cloudflare-Tunnel-to-laptop plan).** From Day 8+ (after cluster is provisioned), Motorola debug APK points `CHAT_BASE_URL = "agent.rishi.yral.com"`. Route: Cloudflare DNS (existing wildcard `*.rishi.yral.com`) → rishi-1/2 Caddy (existing Rishi-owned setup via `yral-rishi-hetzner-infra-template`) → reverse-proxies to rishi-4/5 Swarm ingress. No Cloudflare Tunnel needed — we use the real production-shape routing from Day 8 onwards. Days 1-3 = laptop-local Docker Compose for template dev (no Motorola testing during this window). Days 4-7 = cluster provisioning (still no Motorola). Day 8 = first real APK-to-cluster test. APK changes: ONLY `CHAT_BASE_URL` swap for feature parity phase; per A12 local + never pushed | 🔒 | Rishi 2026-04-24 evening (Q2 answer: Rishi owns Caddy, authorized; Q1 answer: refined hybrid) | Day 8 exit criterion = Motorola debug APK sends a message to `agent.rishi.yral.com`, gets a response from v2 public-api on rishi-4/5, full round trip works |
| A16 | **Feature parity FIRST, mobile code changes DEFERRED until Phase 3+ (locked 2026-04-24 evening).** V2 endpoints return the SAME JSON shapes as chat-ai through the feature-parity window (~Day 8 through Day ~28). Mobile debug APK during this window needs ZERO code changes — only `CHAT_BASE_URL` swap in `AppConfigurations.kt`. This lets Motorola test v2 end-to-end for 3-4 weeks against the real cluster without touching Sarvesh's codebase. Mobile code changes (SSE streaming, presence heartbeat, chip dismissal, new screens for private content, etc.) start Phase 3+ — one change at a time per A12, documented in `mobile-client-change-log.md`, never pushed to origin until Rishi walks Sarvesh through the bundle | 🔒 | Rishi 2026-04-24 evening (Q3 answer) | V2 public-api contract tests verify every chat-ai endpoint returns byte-identical or superset-compatible JSON. First mobile code change (SSE parser, change #1) happens around Day 29 per TIMELINE Phase 3 |

## Category B — Naming & Readability (the "Rishi must be able to read it")

| # | Constraint | Status | Source | How we verify |
|---|---|---|---|---|
| B1 | **Every name reads as English** to a non-programmer. Applies to: service names, DB tables, DB columns, Python functions, Python vars, env vars, Docker stacks/services/secrets/configs/networks/volumes, GitHub repos/branches, Docker image tags, GitHub Actions jobs, Caddy snippets, Prometheus metrics, Grafana panels, Langfuse trace names, Sentry project names, Uptime Kuma monitors, alert rule names, log field names | 🔒 | `feedback_explicit_naming.md`; Rishi 2026-04-22/23 (restated as "very very important") | CI lint block-lists abbreviations; PR template asks reviewer "do all names read as English?"; template CLAUDE.md opens with this rule |
| B2 | **Only allowed abbreviations**: `id`, `url`, `api`, `http`, `json`, `sql`, `utc`, `tls`, `dns`, `ssl`, `css`, `html`, `uuid`, `ip`. Nothing else. `db` → `database`, `cfg` → `config`, `svc` → `service`, etc. | 🔒 | `feedback_explicit_naming.md` | CI lint regex block-list |
| B3 | **V2 service names** follow `yral-rishi-agent-<explicit-english-purpose>` pattern — must include `rishi` (owner), `chat-ai-v2` (version), English purpose | 🔒 | `feedback_explicit_service_naming_v2.md` | `new-service.sh` refuses names that don't match regex or exceed 63-char Swarm limit |
| B4 | **Use DOLR product vocabulary** — "Soul File" (not "system prompt"), "AI Influencer" (not "bot"), "Chat as Human" (exact phrase), "Default Prompts", "Message Inbox", "Switch Profiles" | 🔒 | `reference_yral_soul_file_terminology.md`; `dolr-ai/yral/blob/main/context-for-agents.md` | Code review catches deviations; template docs use these terms exclusively |
| B5 | **Naming readability extends to EVERY artifact**: Swarm stack files, docker-compose, GitHub Actions YAML (job + step names), Bash deploy scripts (variable + function names), Caddy snippets, Patroni configs, Prometheus alert rule names, Grafana panel titles, folder structures inside repos, every YAML key the user can choose. Reinforced 4 times by Rishi | 🔒 | Rishi 2026-04-23 evening; `feedback_naming_extends_to_every_artifact.md` | CI lint extends to YAML key names + Bash variable names; PR template asks reviewer to confirm "could a non-programmer read every name in this PR?" |
| B6 | **Banned generic abbreviation: `utils/`, `helpers/`, `misc/`, `common/`** as folder/file names without a descriptive qualifier. Tells you nothing about contents. Rename to descriptive purpose (e.g., `string-formatting-helpers/`, `date-conversion-helpers/`) | 🔒 | Rishi 2026-04-23 evening | CI lint blocks these names |
| B7 | **3-tier code documentation standard for a non-programmer + ADHD reader (locked 2026-04-27).** Every code file must support reading at three depth levels: (Tier 1) skim the file-header block to know what the file does in 30s; (Tier 2) read function/class headers to know what each piece does in 1-2 min; (Tier 3) read line-level role-comments to understand every line's role in the bigger picture. Specifically required per file: (a) MANDATORY top-of-file header block with: one-sentence summary, plain-English explanation for non-programmer, how-it-fits-with-other-files, inputs/outputs/side-effects, "⭐ START HERE" pointer to the most important function. (b) Each function: 2-3 line WHAT description + WHEN-called + WHY-it-exists comment block, plus line-by-line role-comments in the body. (c) Each class: lifecycle + key methods. (d) Imports: 1-line each explaining what they give us. (e) Functions in PRIORITY order (entry-point first, helpers after, edge cases last) — NOT alphabetical. (f) RELATED FILES footer pointing to next files to read. **Comments explain ROLE in the bigger picture, NOT syntax** (e.g., `# Open the door to the database — rest of function depends on this` GOOD; `# Connects to asyncpg` BAD because it just restates the code). Per-service additions beyond the existing 5 required docs (F8): (g) `WALKTHROUGH.md` — narrative form tracing a single user action through the code, file by file, with line numbers. (h) `GLOSSARY.md` — plain-English definitions of every domain term used in this service. (i) `WHEN-YOU-GET-LOST.md` — 1-page recovery doc with restaurant/pantry analogy + "north star" orientation. Mitigation for line-by-line maintenance burden: CI lint enforces comment density on non-trivial lines; AI agents (Claude/Codex) re-comment as part of every code change via PR template + CI gate | 🔒 | Rishi 2026-04-27; extends `feedback_documentation_standards.md` | CI lint: refuse merge if any function lacks WHAT/WHEN/WHY block; refuse merge if file lacks header block; comment-density check (>50% of non-trivial lines have role comments); 3 new required docs (WALKTHROUGH, GLOSSARY, WHEN-YOU-GET-LOST) checked alongside the existing 5 |

## Category C — Infrastructure & Servers

| # | Constraint | Status | Source | How we verify |
|---|---|---|---|---|
| C1 | **V2 cluster lives on rishi-4/5/6.** Legacy rishi-1/2/3 untouched | 🔒 | Rishi 2026-04-22; Saikat allocation 2026-04-23 | Every new deploy targets rishi-4/5/6 only; Swarm manager runs on rishi-4/5/6 with its own manager token |
| C2 | **Docker Swarm** — orchestrator for v2. No Kubernetes unless Rishi explicitly upgrades later | 🔒 | Rishi 2026-04-22 | Swarm manifests; no K8s YAML anywhere |
| C3 | **Swarm-only networking. No host ports exposed except `:443` on edge (via Swarm ingress).** All inter-service traffic on overlay. Three encrypted overlays: `yral-v2-public-web`, `yral-v2-internal`, `yral-v2-data-plane` | 🔒 | Saikat directive 2026-04-23 (captured in V2_TEMPLATE_AND_CLUSTER_PLAN §1.7) | CI lint (`yq`) rejects any stack file publishing a port other than the edge Caddy 443; UFW rules confirm only 443 open on rishi-4/5 |
| C4 | **Sentry for v2 = `sentry.rishi.yral.com`** (Rishi's self-hosted on rishi-3), NOT `apm.yral.com` (team-shared) | 🔒 | Rishi 2026-04-23 | Every service's SENTRY_DSN points at sentry.rishi.yral.com |
| C5 | **Caddy routing via rishi-1/2 as edge** to rishi-4/5. Cloudflare DNS stays as-is (no new records); rishi-1/2 Caddy reverse-proxies specific subdomains to v2 cluster | 🔒 | Saikat confirmed 2026-04-23 (V2_TEMPLATE_AND_CLUSTER_PLAN §4) | Caddy snippets on rishi-1/2 drop in via SSH as `deploy` user; `caddy reload` zero-downtime |
| C6 | **No literal IPs in code/templates/scripts/CI.** IPs live in `cluster.hosts.yaml` (shape only) + GitHub Secrets (`RISHI_N_PUBLIC_IPV4`). Render step merges at deploy time. **IPs in the plan doc ARE fine** — this rule is about dynamism of deployed systems, not documents | 🔒 | `feedback_no_hardcoded_ips.md`; Rishi 2026-04-23 (clarified docs OK) | CI regex blocks literal IPs in committed code; PR template asks "any literal IPs in this PR?" |
| C7 | **Maximum dynamism / no hardcoded values in code.** Anything shared across services lives in one config that all services read (shared-config.yaml); versions, timeouts, thresholds, model names, all configurable | 🔒 | Rishi 2026-04-23 | Template `shared-config.yaml`; every service reads from it; CI lint for new magic numbers in code |
| C8 | **SSH access via `~/.ssh/rishi-hetzner-ci-key`** for all 6 nodes. `rishi-deploy` user on rishi-4/5/6 (matches legacy convention). Narrow sudoers rule (docker + specific systemctl + journalctl + apt); `root` is break-glass only | 🔒 | `reference_saikat_server_allocation.md` | SSH tests; sudoers file content reviewed |
| C9 | **Datacenter placement**: rishi-1/2/3/4 = Falkenstein (FSN1, confirmed by IP range). rishi-5 ambiguous. rishi-6 likely Nuremberg (NBG1). **Verification deferred to day-0 provisioning** (Rishi choice 2026-04-23, option c — check `/etc/hetzner-provision` + `cat /proc/mdstat` on first SSH). If rishi-6 is cross-DC: Patroni sync replica stays in FSN1 (rishi-4+5); rishi-6 runs async-only | 🔒 | V2_TEMPLATE_AND_CLUSTER_PLAN §5 + §10 + Rishi confirm 2026-04-23 | Day-0 checklist item; Patroni topology adjusted based on result before any data is written |
| C10 | **TLS terminates on rishi-1/2 Caddy**, not on v2 cluster. rishi-1/2 already has ACME certs for `*.rishi.yral.com` from existing setup; they forward-proxy HTTPS traffic to rishi-4/5 over the public network (same-datacenter ~1ms). Rishi-4/5 Caddy (Swarm service) receives HTTPS from rishi-1/2 and forwards to overlay. **No ACME on v2 cluster at Phase 0-1.** Future migration path if needed: add DNS-01 via Cloudflare API on v2 cluster | 🔒 | Rishi 2026-04-23 (Q7 option c) | Caddy snippet on rishi-1/2 does `reverse_proxy https://rishi-4:443 https://rishi-5:443 { health_uri /health/ready }`; rishi-4/5 Caddy inside Swarm serves the request |
| C11 | **Redis HA via Sentinel** (not Cluster). Primary on rishi-4, replica on rishi-5, Sentinel quorum on rishi-4/5/6. Single dataset (non-sharded); one primary for all writes. All Python services use `redis.sentinel.Sentinel` client to discover current primary. Sentinel auto-fails-over on primary loss. Fits easily in 8-16GB RAM at Month-12 projection | 🔒 | Rishi 2026-04-23 (Q8 option a) | Redis stack in bootstrap folder; primary+replica+sentinel containers; template ships with Sentinel-aware `redis_client.py`; fail-over verified in Phase 0 chaos tests |

## Category D — Secrets, Backups, Observability

| # | Constraint | Status | Source | How we verify |
|---|---|---|---|---|
| D1 | **GitHub Secrets primary per-service, Vault only for team-shared** (notification key etc.). No new secrets in Vault. Runtime injection via env vars populated from Swarm secrets. Never in images, git, CI yaml body | 🔒 | `feedback_secrets_github_primary_vault_shared.md` | `gh secret set` for per-service; `infra.get_secret()` for shared; CI gitleaks scan |
| D2 | **Three-layer backup strategy.** L1 Patroni HA (sync commit on ≥1 replica), L2 WAL-G continuous PITR to Hetzner S3 (7-day retention), L3 daily pg_dump to Hetzner S3 (30-day) + weekly to Backblaze B2 (3-month offsite) + monthly encrypted cold (1-year). Weekly restore drill + quarterly DR simulation | 🔒 | `feedback_three_layer_backup.md` | Restore drill is a CI job; failure pages team |
| D3 | **All v2 services tagged `service=<name>` in Sentry** and `environment=production` or `environment=staging` | 🔒 | V2_TEMPLATE_AND_CLUSTER_PLAN §5 staging | Sentry project config; every service emits tagged events |
| D4 | **Langfuse self-hosted on rishi-6** traces every LLM call (prompt, response, tokens, latency, cost). Joinable to Sentry + Prometheus via correlation ID | 🔒 | V2 plan §2.5 + V2_TEMPLATE_AND_CLUSTER_PLAN §6.5 | Template middleware auto-traces; absent = CI fail |
| D5 | **Uptime Kuma monitors at `status.yral.com`** hit every service's `/health/ready`; self-service monitor creation needs Saikat access confirm | 🟡 | V2_TEMPLATE_AND_CLUSTER_PLAN §10 Q5 | New service spawn auto-registers via Kuma API |
| D6 | **Alertmanager destination: Google Chat webhook** — same mechanism current chat-ai uses for admin notifications. All critical alerts (replicas down, Patroni lag, latency regression, disk full, LLM provider errors, backup failures) route here. Team-wide visibility for Rishi + Saikat | 🔒 | Rishi 2026-04-23 (Q9 option a) | Alertmanager config in cluster-bootstrap folder uses Google Chat webhook URL from GitHub Secret `GOOGLE_CHAT_WEBHOOK_URL` |
| D7 | **GitHub Secrets driven by declarative manifest file** at `bootstrap/secrets-manifest.yaml`. Every secret every service needs is declared there with metadata (description, source, used-by). Values still live in GitHub Secrets / Vault, never in the manifest. Bootstrap scripts validate + interactively create missing secrets. CI gate refuses deploy if a required secret is missing | 🔒 | Rishi 2026-04-23 evening; `feedback_secrets_declarative_manifest_pattern.md` | Manifest is source of truth; `bootstrap/scripts/sync-github-secrets.sh` and `validate-secrets-for-this-service.sh` enforce. Replaces the existing template's hardcoded `gh secret set` block with manifest-driven generation |
| D8 | **Per-service `secrets.yaml` manifest in EVERY service folder (locked 2026-04-28).** Companion to D7 cluster-level manifest. Each service spawned from the v2 template gets its own `secrets.yaml` at root that declares every secret THAT service needs (no values, just declarations: name + description + required_in[local,ci,production] + source per-env + rotation_policy + consumed_by[code-files] + classification[blast_radius, access_pattern]). Template ships 3 bridge scripts in every service: `validate-secrets.sh` (CI + local), `sync-github-secrets.sh` (interactive populator), `gen-env-example.sh` (auto-regen .env.example from secrets.yaml). `.env.example` is auto-generated and committed; `.env.local` is gitignored. CI workflow `lint-secrets-hygiene.yml` runs on every PR — fails if .env.example drifts from secrets.yaml OR if any required GitHub Secret is missing. **Per-service production secrets flow: GitHub Secret → Swarm secret at deploy → env var at container start (D1 unchanged). Vault is for SHARED secrets only.** Rishi reaffirmed D1 unchanged 2026-04-28 | 🔒 | Rishi 2026-04-28 | Documented in `secrets-management-pattern-for-every-v2-service/` folder (00-the-pattern, 01-schema-and-example, 02-bridge-scripts). `new-service.sh` scaffolds all of the above when spawning a service. Pre-commit hook scans for accidental secret-shaped strings in committed files |

## Category E — Performance & Product

| # | Constraint | Status | Source | How we verify |
|---|---|---|---|---|
| E1 | **V2 MUST be at least 50% FASTER than Python yral-chat-ai — HARD constraint (2026-04-24, upgraded from "never regress").** Measured on user-interactive endpoints (chat screen load, conversation list, send message, streaming first-token, WebSocket inbox). Admin / list / ban / infrequent endpoints hold to "≤ baseline" (the prior rule). Baseline source: `sentry.rishi.yral.com` Sentry API (key delivered 2026-04-24) pulling yral-chat-ai perf daily. Stretch target: match world-class AI companion apps (Character.ai, Replika, Talkie — published TTFT 200-400ms, full msg 1-3s). Every architectural decision flows from this: if it doesn't help us be 50% faster, it doesn't ship. Users are reporting yral-chat-ai is SLOW — v2 must fix that | 🔒 | Rishi 2026-04-24 (upgraded from 2026-04-23 "never regress"); `feedback_latency_never_regresses.md` (content updated to reflect 50%-faster) | Sentry API script pulls p50/p95/p99 daily, stored in `latency-baseline-capture-from-live-services-the-numbers-v2-must-beat/`. V2 CI latency gate: block merge if any user-interactive endpoint is slower than `0.5 × python_chat_ai_baseline_p95`. Hourly comparison job in prod; auto-rollback on regression |
| E2 | **Streaming in v2.0** — SSE first token <200ms at p95. Ktor SSE parser + Firebase Remote Config flag + fallback to JSON path | 🔒 | Rishi 2026-04-23 | Synthetic user heartbeat + Langfuse time-to-first-token metric |
| E3 | **LLM-agnostic by design.** Orchestrator talks to `llm-client` abstraction; switching providers = config change not rewrite | 🔒 | `feedback_llm_agnostic_design.md` | One trait, many impls; feature flags route per archetype |
| E4 | **No unit-economics cost controls until product-market fit.** Spend whatever needed on LLMs and infra. BUT a runaway-protection circuit breaker IS active with a very high per-user daily cap (e.g., ₹500/user/day). Per-turn cost tracked in Langfuse for visibility; runaway breaker only fires on clear abuse/bug cases | 🔒 | `feedback_no_cost_controls_until_product_market_fit.md`; Rishi 2026-04-23 (resolved: option C — track + active-but-high cap as runaway protection only) | Langfuse tracks cost per turn; circuit breaker exists in template but cap is set 10-100× expected normal user cost. Prevents a runaway loop or compromised user from draining $10K before anyone notices |
| E5 | **H2H chat + Chat as Human + H2AI all same system** from day 1. Unified `conversation_type` column; `participant_b_id` scaffolding preserved | 🔒 | DOLR context doc; current schema already supports | Data model in `yral-rishi-agent-conversation-*` schema |
| E6 | **Auth via `auth.yral.com` (yral-auth-v2)** — OAuth2/PKCE + RS256 JWT validated against JWKS (fixing Ravi's `insecure_disable_signature_validation` gap) | 🔒 | `reference_yral_auth_billing_architecture.md`; Rishi 2026-04-23 | Public-api middleware fetches JWKS, caches in Redis 1hr, validates signature |
| E7 | **Billing via `yral-billing` (Google Play IAP)** — pre-chat access check, ₹9/24hr per bot. Paywall is NOT a 402 response; it's `ApiResponse<ChatAccessDataDto{hasAccess,expiresAt}>` returned before the chat call | 🔒 | `reference_yral_auth_billing_architecture.md`; `reference_yral_mobile_architecture.md` | Orchestrator calls yral-billing before each turn; response shape matches exactly |
| E8 | **Soul File (layered)** is the product term; 4 layers: global / archetype / per-influencer / per-user-segment | 🔒 | `reference_yral_soul_file_terminology.md` | Schema uses `soul_file_*` names; service named `yral-rishi-agent-soul-file-library` |
| E9 | **JWT signature validation in v2: implement JWKS, default OFF, dual-validate during shadow.** V2 public-api implements proper RS256 signature validation against `auth.yral.com/.well-known/jwks.json` (cached in Redis 1hr TTL). BUT a feature flag `enable_strict_jwt_signature_validation` defaults FALSE on day 1 — matches current Python+Rust behavior where signature validation is intentionally disabled. During shadow phase, every JWT is validated BOTH ways (with-sig + without-sig); divergences logged to Sentry. Once divergence-rate < 0.01% for 7 consecutive days, flag flipped TRUE. Risk of enabling without coordination = users get 401; this rollout pattern eliminates that risk | 🔒 | Claude decision 2026-04-23 (audit revealed BOTH services skip sig validation, not just Ravi's; Rishi authorized "go with what feels right") | Public-api auth middleware ships JWKS resolver from day 1; feature flag controls enforcement; Sentry dashboard tracks divergence rate; flip-the-flag is a config change, not a deploy |

## Category F — Template & Service Workflow

| # | Constraint | Status | Source | How we verify |
|---|---|---|---|---|
| F1 | **Template-first build order.** New v2 template (`yral-rishi-agent-new-service-template`) built and proven via a throwaway hello-world service BEFORE any real v2 service ships | 🔒 | `feedback_template_first_build.md` | Phase 0 exit criterion; hello-world must pass all CI lints + deploy + health + Sentry + Langfuse + Beszel |
| F2 | **Existing `yral-rishi-hetzner-infra-template` is NEVER modified.** V2 template forks and evolves independently | 🔒 | No-delete covenant | Existing repo stays frozen; CI doesn't touch it |
| F3 | **Schema-per-service on ONE shared Patroni cluster** on rishi-4/5/6 (not per-service Patroni). Tenant SQL creates schema + role + GRANTs + connection cap at bootstrap. Correlated-failure risk on cluster outage accepted — mitigated by HA + 3-layer backup + chaos tests | 🔒 | V2_TEMPLATE_AND_CLUSTER_PLAN §7.2 + Rishi confirm 2026-04-23 (option a) | Service bootstrap script; each service sees only its own schema |
| F4 | **Staging via namespace separation** (shared infra). ONE Patroni + ONE Redis + ONE Langfuse serve both environments. Separation: `staging_agent_*` schemas, `staging:` Redis key prefix, `environment=staging` Sentry/Langfuse tag. 1-replica per service at 50% prod resources. Weekly reseed from redacted prod snapshot via WAL-G restore | 🔒 | V2_TEMPLATE_AND_CLUSTER_PLAN §5 + Rishi confirm 2026-04-23 (option A) | Every service has staging deploy at `<svc>.staging.v2.rishi.yral.com`; manual "promote to prod" button in GitHub Actions |
| F5 | **arq** as blessed worker library (Redis-backed async queue, fits FastAPI/asyncio, ~1500 LOC) | 🟡 | V2_TEMPLATE_AND_CLUSTER_PLAN §7.3 | Template ships arq-ready; all workers use it |
| F6 | **MCP (Anthropic Model Context Protocol)** as the tool-runtime standard from day 1. Official `mcp` Python SDK | 🔒 | V2 plan §2.1 + V2_TEMPLATE_AND_CLUSTER_PLAN §7.3 | Template helper wraps MCP SDK with correlation-ID + Langfuse |
| F7 | **Cluster-bootstrap folder is a SIBLING at monorepo umbrella root**, NOT inside the template. Structure: `bootstrap-scripts-for-the-v2-docker-swarm-cluster/` (sibling of `yral-rishi-agent-new-service-template/`, sibling of the 13 service folders, sibling of `yral-rishi-agent-plan-and-discussions/`) contains node bootstrap scripts, systemd units, UFW rules, `cluster.hosts.yaml`, `services.yaml`, `secrets-manifest.yaml`, render scripts. Updated 2026-04-24 from earlier "inside template" position per Rishi's audit-Q1 answer | 🔒 | Rishi 2026-04-24 (audit-Q1: "Keep as sibling at umbrella root") | Folder exists at `~/Claude Projects/yral-rishi-agent/bootstrap-scripts-for-the-v2-docker-swarm-cluster/`; `services.yaml` lives there; CI workflows in `.github/workflows/` reference path-scoped triggers per-service |
| F8 | **8 required docs per service (UPGRADED 2026-04-27 from 5 to 8 per B7).** Inherited 5: DEEP-DIVE / READING-ORDER / CLAUDE / RUNBOOK / SECURITY. New 3 added 2026-04-27 for non-programmer reading: WALKTHROUGH (narrative trace of one user action through code), GLOSSARY (plain-English definitions of every domain term), WHEN-YOU-GET-LOST (1-page north-star orientation with restaurant/pantry analogy). All 8 are CI-gated | 🔒 | `feedback_documentation_standards.md` + B7 (Rishi 2026-04-27) | Template scaffolds all 8 with `(STUB)` placeholders that fail CI until filled; new-service.sh refuses to mark a service "ready" until all 8 are non-stub |
| F9 | **Uniform `/health` three-tier split** — `/health/live` (process alive), `/health/ready` (deps healthy, 503 if not), `/health/deep` (real round-trip, expensive) | 🔒 | V2_TEMPLATE_AND_CLUSTER_PLAN §7.3 | Swarm uses `ready` for health checks; Uptime Kuma uses `ready`; synthetic user uses `deep` |
| F10 | **Idempotency-key default-on** on all non-GET endpoints; dedupes via Redis 24hr TTL. Per-endpoint opt-out for truly stateless | 🔒 | V2_TEMPLATE_AND_CLUSTER_PLAN §7.3 | Middleware in template; CI test covers dedup |
| F11 | **Feature flags custom Postgres-table**, ~200 LOC, polled every 30s, on/off + % rollout. Upgradeable to Unleash if experimentation grows | 🔒 | V2_TEMPLATE_AND_CLUSTER_PLAN §7.3 | Every new feature ships behind a flag |
| F12 | **Python 3.12+ + FastAPI + asyncio + asyncpg uniformly across all 13 services.** Same stack as existing template. No Go/Rust unless a specific service later proves CPU-bound under load. One language = one mental model for a non-programmer Rishi + Claude/Codex agents to navigate | 🔒 | Rishi 2026-04-23 (Q6 option a) | Template `pyproject.toml` pins Python version; CI rejects non-Python service additions without explicit exception |
| F13 | **Container registry: GHCR (GitHub Container Registry).** Images published as `ghcr.io/dolr-ai/<service-name>:<git-sha>`. Auth via GitHub Actions built-in `GITHUB_TOKEN` — zero extra config. Same pattern as existing template | 🔒 | Rishi 2026-04-23 (Q11 option a) | Every service's `Dockerfile` + deploy workflow pushes to GHCR; rishi-4/5/6 pulls cached per-node after first pull |
| F14 | **Eval harness: Langfuse built-in eval framework.** Tight integration with our LLM tracing (we're already self-hosting Langfuse). Eval runs store alongside real traces; can correlate production quality with eval scores. Held-out gold prompt set in `evals/` folder per service; CI runs on every PR touching LLM-facing code and posts diff to PR. Migration option to Promptfoo later if we outgrow it | 🔒 | Rishi 2026-04-23 (Q13 option a) | Template ships `evals/` folder + CI job + PR comment bot; first gold prompt set curated from current prod chat-ai "best" and "worst" conversations |
| F15 | **Refined capability priority order (Rishi 2026-04-23 evening):** supersedes earlier slicing. **(1) Memory + Depth → (2) Better Quality / Response Quality → (3) Billing integration → (4) Proactivity → (5) Programmatic AI influencer creation via open API + MCP-like service → (6) Real influencer parity → (7) First-turn nudge → (8) Creator tools + analytics → (9) Creator monetization + private content → (10) Meta-AI advisor + automated analytics.** Detail in `refined-capability-priority-order-and-slicing/priority-order-locked-2026-04-23.md`. Weeks are aspirational only (cutover is NOT tied to any phase). | 🟡 | Rishi 2026-04-23 evening — provisional until Rishi refines further | Plan roadmap phases re-sequenced to match; each service's design doc tagged with the priority number |
| F16 | **Monorepo architecture LOCKED (Rishi 2026-04-24):** ONE public GitHub repo `dolr-ai/yral-rishi-agent` holds EVERYTHING — `yral-rishi-agent-plan-and-discussions/` folder + `yral-rishi-agent-new-service-template/` folder + 13 service folders + `shared-library-code-used-by-every-v2-service/` + `bootstrap-scripts-for-the-v2-docker-swarm-cluster/`. V2 template's `new-service.sh` creates a new SUBFOLDER in the monorepo, NOT a new GitHub repo (adapts existing template's polyrepo pattern). Path-scoped CI per service. Monorepo chosen over polyrepo because: one mental model (ADHD-friendly), shared code in one place (auth / LLM client / Langfuse wiring), atomic cross-service refactors, matches `yral-mobile` precedent, aligns with dolr-ai's 94%-public org norm | 🔒 | Rishi 2026-04-24 | `feedback_monorepo_architecture_locked.md`. Repo publicly visible at `github.com/dolr-ai/yral-rishi-agent` (renamed from `yral-rishi-agent-plan`, full history preserved via GitHub auto-redirect) |

## Category G — Scale & Projections

| # | Constraint | Status | Source | How we verify |
|---|---|---|---|---|
| G1 | **Scale targets**: today ~25K msgs/day; Month 6 ~300-500K; Month 12 ~1M+. Architecture must hold at 40× without re-architecture | 🔒 | `project_yral_scale_projection.md` | Every architectural choice stress-tested against Month 12 projection |
| G2 | **Horizontal scaling from day 1.** Every service stateless-by-default (except Patroni/Redis/Langfuse); Swarm replicas handle scale-out | 🔒 | V2 plan §2.7.5 | Template ships with 3-replica default for hot path |
| G3 | **pgBouncer in front of Patroni from day 1.** Trivial to add now, painful later | 🔒 | V2_TEMPLATE_AND_CLUSTER_PLAN §7.2 | Deployed in Phase 0 stateful core |
| G4 | **Multi-LLM-provider routing by Month 6-9** (when single-provider rate limits hit). Day-1 template has the abstraction ready | 🔒 | `feedback_llm_agnostic_design.md` | `llm-client` trait with multiple impls shippable via config flip |

## Category H — Reliability & Safety

| # | Constraint | Status | Source | How we verify |
|---|---|---|---|---|
| H1 | **`yral-v2-swarm-resync.service`** systemd oneshot runs after docker.service to idempotently redeploy all stacks (fixes April 19 `restart:always` unreliable-on-reboot pattern) | 🔒 | `reference_docker_restart_policy_edge_case.md`; V2_TEMPLATE_AND_CLUSTER_PLAN §6.1 | Phase 0 chaos test: reboot rishi-6, every stack back within 2 min |
| H2 | **SHA-rotating Swarm configs** — `name: <stack>_<configname>_<sha8>` + prune old (per the April 20 bug fix pattern) | 🔒 | `reference_template_haproxy_cfg_bug.md` | Every config has SHA suffix; old ones pruned post-rollout |
| H3 | **Chaos tests as Phase 0 exit criteria** — kill rishi-6 (drain), kill rishi-4 Patroni container, fill rishi-5 disk 80%, partition rishi-6 from 4/5 for 10 min. All must pass before any real service runs | 🔒 | V2_TEMPLATE_AND_CLUSTER_PLAN §6.7 | Chaos test scripts in cluster-bootstrap repo; Phase 0 gate |
| H4 | **Crisis detector + content safety ship before canary traffic.** Non-negotiable for mental-health-adjacent chat | 🔒 | V2 plan §6 principle 12 | `yral-rishi-agent-content-safety-and-moderation` live + integrated before any real user hits v2 |
| H5 | **Prompt injection defense middleware** pre-orchestration. Blocks extraction attempts, logs to Sentry with `type=prompt_injection`, returns safe fallback | 🔒 | V2_TEMPLATE_AND_CLUSTER_PLAN §7.3 | Middleware in public-api; tests include known injection payloads |
| H6 | **PII-aware log redaction.** Message bodies, user names, email, phone NEVER in Loki, Sentry breadcrumbs, Langfuse trace payloads. Structured logger with allow-list of safe fields | 🔒 | V2_TEMPLATE_AND_CLUSTER_PLAN §7.3 | Code review + CI grep for sensitive field names in log statements |
| H7 | **Shadow traffic middleware** for every new orchestrator change — mirrors real requests to candidate, compares responses offline in Langfuse | 🔒 | V2 plan §2.1 + V2_TEMPLATE_AND_CLUSTER_PLAN §7.3 | Deploy flow: every orchestrator PR runs shadow for N days before promote |
| H8 | **Eval harness baked into template.** Held-out 200-prompt set + promptfoo-style runner; CI runs on every PR touching LLM paths; diff posted to PR | 🔒 | V2 plan §2.1 + V2_TEMPLATE_AND_CLUSTER_PLAN §7.3 | `evals/` folder in template; CI job + PR comment bot |
| H9 | **Synthetic user heartbeat** — one canary bot per env sends real API turn every 5 min via real auth; alerts on failure, latency >2× baseline, or eval-metric degradation | 🔒 | V2_TEMPLATE_AND_CLUSTER_PLAN §7.3 | Container deployed in Phase 0; Grafana dashboard tracks |
| H10 | **Dead letter queue for workers.** arq retries 3× with jittered backoff, then DLQ stream `worker.dlq` with full context. Alert on depth >100 or age >1h | 🔒 | V2_TEMPLATE_AND_CLUSTER_PLAN §7.3 | Template ships DLQ; Grafana panel + alert |
| H11 | **Schema migration safety net** — every migration PR auto-runs against WAL-restored yesterday-prod snapshot + full test suite; blocks merge on failure | 🔒 | V2_TEMPLATE_AND_CLUSTER_PLAN §7.3 | CI job; uses L2 backup infra |

## Category I — Process

| # | Constraint | Status | Source | How we verify |
|---|---|---|---|---|
| I1 | **Plan-only until explicit Rishi "build" instruction.** No repo creation, no server bootstrap, no code writing for v2 services | 🔒 | `feedback_plan_only_until_explicit_build_approval.md` | All actions that would materialize plan into running systems require typed approval |
| I2 | **Canary deploy + auto-rollback** per service on every deploy: rishi-4 first → health check → rishi-5 → health check → rishi-6. Failure = rollback to last-good image tag | 🔒 | Existing template pattern | Deploy workflow in template; all services inherit |
| I3 | **Manual "promote to prod"** — staging auto-deploys on push to main; prod requires clicking GitHub Actions button after staging smoke + latency gate green | 🔒 | V2_TEMPLATE_AND_CLUSTER_PLAN §6.6 | Workflow exists; no auto-prod deploys |
| I4 | **15-min retrospective after each new service build** — what did we copy-paste? fold into template. What broke twice? fix in template. What doc gap? update template docs | 🔒 | `feedback_template_first_build.md` | Retrospective is part of "done" for each service |
| I5 | **Test locally before CI** (existing rule) — for fixes to current dolr-ai services, reproduce on local stack before pushing to main (which auto-deploys) | 🔒 | `feedback_test_locally_before_ci.md` (existing memory) | Applies to any hotfix we might do on existing services during v2 build |
| I6 | **Claude MUST push back on non-industry-standard or likely-wrong decisions.** Rishi is a non-programmer with ADHD — he explicitly does not always take the right call and asks for pushback. Scope: technical, architectural, product, UX — all of the above. Format: (a) state the specific concern in one sentence, (b) give the industry-standard or better alternative, (c) explain the tradeoff in concrete terms (risk / cost / reversibility / time), (d) let Rishi decide. Do NOT silently agree when you spot a problem; do NOT surrender on first objection — push back once with reasoning, then accept Rishi's call. Rishi retains final authority. This applies to ALL of v2 work, not just this plan: memory writes, code edits, infra choices, naming, sequencing | 🔒 | Rishi 2026-04-24 | Every session: when I notice a concern I must name it; PR/plan reviews explicitly flag "concerns I raised" vs "concerns Rishi overrode"; no silent compliance with decisions I think are wrong |
| I7 | **Sentry API access pre-authorized 2026-04-24** for reading perf + error data from `sentry.rishi.yral.com`. Key location TBD (Rishi to confirm — default assumption `~/.config/dolr-ai/sentry-api-key`). Used by: latency baseline capture (daily pull of yral-chat-ai p50/p95/p99), error volume trend tracking, comparison between v1 and v2 during shadow/canary. Raw event data (message bodies) never pulled — aggregated stats only | 🔒 | Rishi 2026-04-24 | Baseline capture script reads key from env var `SENTRY_API_KEY` populated from the file; Sentry queries stored in `latency-baseline-capture-from-live-services-the-numbers-v2-must-beat/` |
| I8 | **3-then-5 parallel Claude Code sessions sharded by ownership scope (refined 2026-04-27).** Day 1 launch: Sessions 1 (Infra & Cluster), 2 (Template & Hello-World), 5 (ETL + Memory + Tests). Day 9 launch: Sessions 3 (Public-API & Auth), 4 (Orchestrator + Soul File + Influencer Directory). Each session writes ONLY inside its assigned subfolder. Coordinator session (Rishi + Claude, this one) holds cross-cutting context: CONSTRAINTS.md, README.md, TIMELINE.md, plan docs, interface-contracts/, .github/workflows/, shared-library. Refinement reason: Sessions 3+4 depend on cluster + template being live; launching them earlier wastes context. Full detail in `multi-session-parallel-build-coordination/01-SESSION-SHARDING-AND-OWNERSHIP.md` | 🔒 | Rishi 2026-04-27 | CI lint workflow `lint-scope-violations.yml` rejects PRs that touch files outside the owning session's scope (inferred from branch name `session-N/*`) |
| I9 | **Auto-mode guardrails** — sessions in Auto-mode may execute only the operations enumerated in `multi-session-parallel-build-coordination/02-AUTO-MODE-GUARDRAILS.md`. Allowed: code in own scope, local tests, docker compose, branch pushes, gh pr create, Sentry API reads. Forbidden (must STOP and ask Rishi via coordinator): pulling live yral-chat-ai DB data (A14), pushing to yral-mobile (A12), writes to rishi-1/2/3 except the explicitly-authorized Caddy snippet via `yral-rishi-hetzner-infra-template` repo (A2 exception), deletions (A1), main-branch merges, edits to CONSTRAINTS/README/TIMELINE (coordinator only), edits to other sessions' scopes, cutover work (A6), memory file writes, GitHub secrets rotation, $10+/month new dependencies. Kill-switch: any session can be halted by typing "STOP AUTO-MODE"; "STOP ALL SESSIONS" halts all 5 | 🔒 | Rishi 2026-04-27 | Defense-in-depth: session prompts + CI lint + branch protection on main + Codex review + coordinator review. Forbidden ops blocked at the prompt layer (session refuses) AND at the CI layer (PRs fail if attempted) |
| I10 | **Codex independent review on every PR before merge (locked 2026-04-27).** GitHub Action `pr-codex-review.yml` triggers on every PR opened to main. Codex receives the diff + CONSTRAINTS.md + session scope + B7 doc standard, returns inline review comments. Coordinator session reads both Claude's PR description and Codex's review, summarizes for Rishi (≤3 lines for routine PRs). Rishi types YES → coordinator merges. Three review categories: (a) hard violations auto-block merge; (b) style/doc concerns Rishi can YES through; (c) design disagreements get coordinator + Rishi decision. Codex API key in GitHub repo secret `OPENAI_CODEX_API_KEY`. Cost ~$120/month at 5-session cadence (within E4 no-cost-controls but flagged). Full detail in `multi-session-parallel-build-coordination/03-CODEX-REVIEW-WORKFLOW.md` | 🔒 | Rishi 2026-04-27 | Branch protection on main: PRs require CI green (incl. Codex review job) + coordinator approval + Rishi YES comment. No direct pushes to main. Auto-merge disabled (Rishi must explicitly YES) |
| I11 | **5-layer state persistence + resume strategy (locked 2026-04-27).** Layer 1 = `session-logs/SESSION-N-LOG.md` append-only diary auto-written via Claude Code PostToolUse hook on every git commit. Layer 2a = `cross-session-dependencies.md` kanban (sessions raise OPEN deps, coordinator moves to RESOLVED). Layer 2b = `session-state/SESSION-N-STATE.md` per-session "where am I now" snapshot, regenerated on every commit. Layer 3 = `MASTER-STATUS.md` at top of coordination folder, auto-regenerated every 15 min by launchd job, the ONE file Rishi reads each morning. Layer 4 = `daily-reports/YYYY-MM-DD.md` end-of-day summary written by coordinator. Layer 5 = `decision-log.md` append-only forever-record of cross-cutting decisions. Hook script: `.claude/hooks/post-tool-use.sh` (auto-appends log entries on commit). Regenerator: `.claude/scripts/regenerate-master-status.sh`. CI lint `lint-state-hygiene.yml` fails PRs that don't update the session's log. Full detail in `06-STATE-PERSISTENCE-AND-RESUME.md` | 🔒 | Rishi 2026-04-27 | MASTER-STATUS shows last-update timestamp prominently; >30 min stale = visible alarm. State files are also git-tracked, so git history is the disaster-recovery source of truth |
| I12 | **Resume protocol on every session start (locked 2026-04-27).** When a session is launched (fresh terminal, after laptop restart, etc.), it MUST: (1) read its own `SESSION-N-STATE.md`, (2) read last 50 lines of `SESSION-N-LOG.md`, (3) filter `cross-session-dependencies.md` to its section, (4) scan `MASTER-STATUS.md` for context, (5) print pre-written CONFIRM-TO-RISHI sentence to terminal, (6) WAIT for Rishi to type "continue" before any Auto-mode action. This is enforced via the session prompt template — sessions that skip resume protocol are pinged by coordinator | 🔒 | Rishi 2026-04-27 | Session startup prompts include the explicit 6-step resume protocol; coordinator audits via "first commit after restart" check |
| I13 | **MASTER-STATUS.md is the single morning-orientation file for Rishi (locked 2026-04-27).** Section order optimized for ADHD reading: (1) ❓ AWAITING RISHI at top, (2) 📊 SESSION HEALTH with emoji status per session, (3) 🤖 AUTO-MERGED IN LAST 24H, (4) 🔗 OPEN CROSS-SESSION DEPENDENCIES, (5) 📈 LATENCY BASELINE, (6) 💰 SPEND, (7) ⏰ TODAY'S CRITICAL PATH. Auto-regenerated every 15 min. Lives at `multi-session-parallel-build-coordination/MASTER-STATUS.md`. Rishi opens this morning + evening; nothing else needed for orientation | 🔒 | Rishi 2026-04-27 | Template at `templates/MASTER-STATUS-TEMPLATE.md`; regen script reads all SESSION-N-STATE files + git log + Codex API spend |
| I14 | **Coordinator auto-merges low-risk PRs without Rishi YES (locked 2026-04-27).** "Low-risk" = ALL of: (a) PR is .md-only OR test-only OR lint/format-only OR comment-update-only; (b) Codex review = APPROVE with no concerns/blockers; (c) all CI checks green; (d) diff size < 200 lines; (e) branch matches `session-N/*` pattern; (f) no files in critical scopes (.github/, secrets, CONSTRAINTS.md, memory files). Coordinator merges with comment "Auto-merged per I14: low-risk + Codex APPROVE." Auto-merge entry surfaces in next MASTER-STATUS in 🤖 AUTO-MERGED section so Rishi sees it within ~15 min. ANY one false → standard Rishi YES flow per I10 | 🔒 | Rishi 2026-04-27 | Coordinator runs auto-merge logic on each PR; decision-log gets one entry per auto-merge; weekly audit confirms no auto-merge overstep |
| I15 | **Each session's role encoded as a project-scoped subagent definition (locked 2026-04-27 evening).** File at `.claude/agents/session-N-<role-name>.md` with frontmatter (name, description, tools, model) + body that includes the session's resume protocol + scope + first-week deliverables. Inspired by Anthropic's `code.claude.com/docs/en/agent-teams` doc — subagent definitions are stable, reusable, version-controlled. Even though we don't use the experimental agent-teams wrapper itself, the subagent-definition primitive is solid and replaces our long startup prompts | 🔒 | Rishi 2026-04-27 evening | `.claude/agents/session-1-infra-cluster.md` etc. exist before launch; sessions reference their definition on spawn |
| I16 | **Task-lifecycle hooks enforce quality gates (locked 2026-04-27 evening).** In addition to the PostToolUse hook (auto-log on commit per I11), add: (a) TaskCreated hook → reject task names violating B1/B5/B6 with feedback to session; (b) TaskCompleted hook → reject completion if SESSION-N-LOG.md wasn't appended for this task; (c) TeammateIdle hook → ping coordinator if session is idle >30 min during work hours. All hooks fire locally on Rishi's laptop, tight loop, catch issues immediately. Hooks live in `.claude/hooks/` and are version-controlled in the monorepo | 🔒 | Rishi 2026-04-27 evening | Hook scripts exit with code 2 on violation, sending feedback to the session; CI lint additionally checks the same conditions on PR (defense in depth) |
| I17 | **Tmux split-pane display mode for multi-session viewing (locked 2026-04-27 evening).** Rishi runs ONE iTerm2 window with `tmux -CC` session named `yral-v2-build`. Coordinator session lives in the top pane (always visible). Sessions 1-5 each live in their own tmux pane. Rishi clicks into a pane to interact with that session; sees all sessions at a glance. ADHD-friendly: no window-switching, no terminal confusion. Setup: `brew install tmux` + iTerm2 already installed. Fallback: in-process mode (single terminal, Shift+Down to cycle) if tmux setup is painful | 🔒 | Rishi 2026-04-27 evening (recommended in `code.claude.com/docs/en/agent-teams`) | Tmux pane layout documented in `05-GETTING-STARTED-TOMORROW.md`; Rishi tests with 2 panes during warm-up to verify before scaling to 6 |
| I18 | **Anthropic's experimental "agent teams" wrapper is DEFERRED (locked 2026-04-27 evening).** Reasons: (a) experimental flag with known limitations; (b) NO `/resume` for in-process teammates after laptop crash — fatal for 6-week build; (c) one team per session, no nesting; (d) uniform permissions across teammates. We adopt the STABLE primitives (subagent definitions per I15, hooks per I16, tmux display per I17, 3-5 teammate sweet spot) without the experimental wrapper. Manual session launch per `05-GETTING-STARTED-TOMORROW.md` continues. Revisit when: (1) Anthropic moves agent teams out of experimental, (2) `/resume` works for teammates, (3) we have 1-2 months of v2 build under our belt to prove our coordination pattern | 🔒 | Rishi 2026-04-27 evening; pushback per I6 against adopting experimental tech for production-adjacent build | `CLAUDE_CODE_EXPERIMENTAL_AGENT_TEAMS` is NOT set; `~/.claude/settings.json` does not enable it; if we revisit, that's a separate explicit decision |

---

## Category J — Testing & Quality Gates

| # | Constraint | Status | Source | How we verify |
|---|---|---|---|---|
| J1 | **Risk-weighted test coverage targets per service (locked 2026-04-29 — Option E).** HOT tier (75-80% floor): public-api, orchestrator, content-safety, payments, etl-scripts. WARM tier (50-60% floor): soul-file-library, user-memory, influencer-directory, skill-runtime. COOL tier (25-35% floor): creator-studio, events-analytics, proactive-scheduler, media-vault, meta-advisor. CI gate blocks merge if coverage on touched files REGRESSES below floor (not absolute floor — the relative regression). Floors are floors not ceilings; natural drift to 90% is fine. Floor adjustments require Rishi YES + decision-log entry. Detail in `testing-strategy-and-quality-gates/01-coverage-targets-per-service.md` | 🔒 | Rishi 2026-04-29 (Option E) | `pytest --cov` + GitHub Action `lint-coverage-floors.yml`; PR comment shows delta + tier + floor for at-a-glance review |
| J2 | **Zero-flake policy — 24-hour fix-or-delete (locked 2026-04-29).** Once a test flakes 2× in 7 days, it gets entered in `tests/flaky-tests-quarantine.md`. Author has 24 hours to fix, make deterministic, or delete. After 24h flaky tests get CI-skipped with reason; after 14 days quarantined they auto-delete unless resurrected. NEVER quarantined: tests for HOT-tier paths (auth, billing, safety, data-correctness), contract tests vs chat-ai, latency CI gate — these MUST be fixed never deleted | 🔒 | Rishi 2026-04-29 | CI tracks per-test failure history; auto-comments on PR after 2nd flake; coordinator escalates to Rishi if author doesn't act in 24h |
| J3 | **AI-generated tests with Codex review (locked 2026-04-29).** Sessions write tests as part of every code-change PR. Codex review prompt includes test-quality check: tautological assertions flagged, missing test coverage flagged, flake patterns (time-dependence, unmocked network, race conditions) flagged. Tests follow B7 doc standard: plain English names, WHAT/WHEN/WHY docstring, file headers, priority order in file (happy path first), role-not-syntax inline comments. Rishi reads only PR-comment summaries — never test code itself | 🔒 | Rishi 2026-04-29 | Codex prompt addendum at `.github/scripts/codex-prompt-test-review.txt`; CI lint enforces B7 standards on test files; coverage trend in MASTER-STATUS |
| J4 | **Smoke tests post-deploy + synthetic user heartbeat (locked 2026-04-29, extends H9).** Every deploy runs a 5-10 endpoint smoke test (health + 3 key paths per service) before marking deploy successful. H9 synthetic user runs full chat turn every 5 min on production, alerts via Sentry on any failure or latency >2× baseline (per E1). Smoke = "is the pipe up?"; synthetic = "does a real user flow work?" | 🔒 | Rishi 2026-04-29 (extends H9) | `tests/smoke/` per service; deploy.yml runs smoke tests; rejects deploy if any smoke fails; H9 synthetic container in cluster |
| J5 | **Pre-commit secret scan (locked 2026-04-29).** Pre-commit hook runs gitleaks + custom secret-pattern regex scanner before any commit lands locally. CI re-runs as defense-in-depth. Forbidden patterns: `sk-`, `ghp_`, `xoxb-`, base64-looking 40+ char strings outside .env.example, JWT format strings outside test fixtures. Caught secret = commit BLOCKED with remediation message ("rotate the leaked credential, then commit"). Per D1 + D8 secrets stay in proper sources (GitHub Secrets, Vault, .env.local) — never in code | 🔒 | Rishi 2026-04-29 | `.pre-commit-config.yaml` ships in template; new-service.sh installs hooks; CI lint workflow `lint-secret-scan.yml` re-runs |
| J6 | **Motorola test as final smoke (locked 2026-04-29).** Subjective UX trumps green tests. Rishi uses the chat 5-10 min/day on his Motorola debug APK during build. If something feels off (slow, weird, broken character, missing memory) — that's a bug report regardless of CI status. Coordinator + AI agents add coverage in the gap between "tests pass" and "Motorola feels right" | 🔒 | Rishi 2026-04-29; aligned with A9 + A15 | Daily Motorola feedback captured in `daily-reports/<date>.md`; gaps logged as test-coverage-additions in next session's PRs |

## Appendix A — Conflicts Resolved 2026-04-23

All four previous conflicts resolved:

1. **Cost-cap vs no-cost-ceiling** → Option C: **runaway-protection cap only**. Track cost per turn in Langfuse (no gating for normal usage). Active circuit breaker with a very high per-user daily cap (e.g., ₹500/user/day) as a safety net against abuse, bugs, or infinite loops — NOT a unit-economics control. Row E4 reflects this.
2. **Shared Patroni vs per-service** → Option A: **shared Patroni, schema per service**. Correlated outage risk accepted — mitigated by HA (3 nodes, sync commit on ≥1 replica), chaos tests (Phase 0 exit criterion), 3-layer backup (L1 HA + L2 WAL PITR + L3 offsite), and schema isolation (per-service role, grants, connection cap). Row F3 🔒.
3. **Staging shared vs isolated** → Option A: **shared infra, namespace separation** (same Patroni / Redis / Langfuse, separated by schema prefix / Redis key prefix / environment tag). Weekly reseed from redacted prod snapshot. Row F4 🔒.
4. **Cluster-bootstrap location** → 2026-04-23 initial decision was "fold into template repo" (Option B). **REVERSED 2026-04-24** per Rishi's audit-Q1 answer to "Keep as sibling at umbrella root." Bootstrap now lives at `bootstrap-scripts-for-the-v2-docker-swarm-cluster/` next to the template + service folders. Row F7 updated 🔒.

All resolutions committed to memory + plan. No outstanding conflicts.

## Appendix B — Open Questions (not yet constraints)

See V2_TEMPLATE_AND_CLUSTER_PLAN §10 for full list. Highlights:

- Disk layout on rishi-4/5/6 (RAID1 preferred; verify day-0 via `/proc/mdstat`)
- Rishi-1/2 Caddy SLA (our v2 availability is coupled; worth asking Saikat)
- Sentry self-service project creation permission on `sentry.rishi.yral.com`
- Rishi-6 datacenter verification (Falkenstein vs Nuremberg) — affects Patroni topology
- Backup off-site pair (Backblaze B2 vs alternatives)

---

*Last updated: 2026-04-23. Review cadence: before any phase transition. Any row's status change requires a git commit with explanation.*
