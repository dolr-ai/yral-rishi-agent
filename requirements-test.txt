# Test-only dependencies. Kept out of requirements.txt so the runtime
# image stays lean. CI installs both; see .github/workflows/ci.yml.
#
# testcontainers spins an ephemeral pgvector/pgvector:pg15 container per
# test session so the integration tests run against a REAL Postgres with
# the real asyncpg codecs — see tests/conftest.py + docs/wave1-plan-2026-07-29.md.
testcontainers[postgres]==4.15.0

# Coverage is a CI ratchet, not a target: ci.yml fails the build if app/ line
# coverage drops below the committed floor. Routes sat at 22% when the floor
# was introduced — see docs/test-quality-audit-2026-09-11.md.
pytest-cov==7.0.0
