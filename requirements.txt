fastapi==0.141.1
# Pinned explicitly, not left transitive: fastapi 0.141 requires only
# `starlette>=0.46.0` with NO upper bound, so a resolver may legitimately
# install the vulnerable 0.46.2 again. This floor is what actually carries
# the CVE fixes (CVE-2026-54283 needs >=1.3.1).
starlette==1.6.0
uvicorn[standard]==0.34.0
asyncpg==0.30.0
pyjwt[crypto]==2.13.0
openai==1.82.0
boto3==1.38.6
httpx==0.28.1
python-multipart==0.0.31
sentry-sdk[fastapi]==2.22.0
python-dateutil==2.9.0.post0
langfuse==2.60.2
redis==5.2.1
Pillow==12.3.0
