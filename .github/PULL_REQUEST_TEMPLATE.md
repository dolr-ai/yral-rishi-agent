## What this PR does (one sentence)


## Why (the problem it fixes, not a description of the diff)


## How I verified it (real behaviour — an HTTP call, the alpha app, a query — not a source-text grep)


## What might break


## Rollback
`gh pr revert <number>` reverts cleanly; no schema or external state to undo. (Edit if that's not true.)
